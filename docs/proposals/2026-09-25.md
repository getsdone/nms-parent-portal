# Retro proposals, 2026-09-25

Accepted into `rules.toml`: design-names-runtime-versions,
fix-brief-covers-full-pipeline, seed-dates-relative-to-run,
optimistic-update-has-revert. Accepted into `CLAUDE.md`: cut worktrees
after the scaffold lands; opus for reviews of shared infrastructure.

Deferred, harness-level, not this repo's to fix:

- `read_only_gate` lets a reviewer add a worktree outside the repo but
  blocks its removal. One reviewer left `/tmp/nms-wp6-review` behind.
- `~/.claude/agents/{reviewer,design-reviewer,retro}.md` are stale against
  the plugin's copies and understate what the gate enforces.
