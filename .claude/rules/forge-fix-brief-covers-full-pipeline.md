---
paths:
  - "server/src/**"
---
<!-- generated by rules.py render from rules.toml; edit the toml -->

- A fix brief that touches middleware registration is scoped to every file that mounts something after it, not only the file with the bug.
